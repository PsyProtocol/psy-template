{
  "name": "psy-nft",
  "version": "1.0.0",
  "description": "PSY-721 Non-Fungible Token Smart Contract Template",
  "type": "module",
  "scripts": {
    "configure": "node scripts/configure_issuer.mjs",
    "check:config": "node scripts/configure_issuer.mjs --check",
    "check:preflight": "node scripts/configure_issuer.mjs --check --strict",
    "build": "psyup build",
    "build:deploy": "npm run check:preflight && psyup build",
    "test": "dargo test --file tests/nft_unit_test.psy"
  },
  "author": "Psy Protocol",
  "license": "MIT"
}
