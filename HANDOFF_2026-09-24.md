# PSY-20 / PSY-721 工程交接（2026-09-24）

## 一页结论

`psy-template` 已实现并在 staging 验证基本的 PSY-20、PSY-721 跨用户流程，但**尚不能标记为生产级标准资产合约**。最重要的阻断项是 PSY-20 的单方即时撤销：当前合约 48 会拒绝读取到撤销后状态的新花费，却仍接受 Bob 在撤销前生成、撤销确认后才提交的旧证明。用户要求的“撤销后额度归零，后续花费不能确认”需要节点在交易确认点校验跨用户读取的最新状态；只改合约额度、状态或 nullifier 不能实现。

代码基线：[`psy-template` main `01f4970`](https://github.com/PsyProtocol/psy-template/commit/01f4970)。本地完整测试为 2 组原生测试和 7 组 E2E，全部通过。`psy-dapp` 的 `multi_chain` 已快进到 `61f87bb`。按当前要求，**不要修改 `psy-node`**；本交接只记录后续需要的协议工作。

## 已完成并有证据的工作

| 范围 | 已验证结果 | 证据 |
| --- | --- | --- |
| PSY-20 基本转账 | 两个不同 realm 的注册用户完成转出、认领、反向转出和认领；余额由状态证明核对 | [跨 realm 报告](tests/live/STAGING_REPORT_2026-09-23.md) |
| PSY-20 委托 | 当前 v3 合约 48 保留 16 个并行托管槽；开通、花费、认领、Bob 关闭、Alice 退款均在 staging 确认 | [委托报告](tests/live/PSY20_DELEGATION_REPORT_2026-09-23.md) |
| PSY-20 撤销边界 | 撤销后才新发起的花费被合约拒绝；撤销前生成的旧证明在撤销后提交仍被确认 | [合约 48 旧证明交易](tests/live/evidence/2026-09-23/token-delegation-v3-active-guard-historical-spend.json)、[最终状态证明](tests/live/evidence/2026-09-23/token-delegation-v3-active-guard-historical-state-proofs.json) |
| PSY-721 基本转移 | v3 合约 44 在两用户间完成铸造、往返转移、认领及 ACK；四 Felt token ID 与元数据保持一致 | [NFT staging 报告](tests/live/STAGING_REPORT_2026-09-23.md)、[状态证明](tests/live/evidence/2026-09-23/nft-v3-state-proofs.json) |
| PSY-721 队列 | 每个“发送者→接收者”方向最多 4 笔**未 ACK**转账；第 5 笔拒绝，认领加 ACK 后可复用环形槽，累计发送量不限于 4 | [队列交易](tests/live/evidence/2026-09-23/nft-v3-capacity.json)、[状态证明](tests/live/evidence/2026-09-23/nft-v3-capacity-state-proofs.json) |

本地单测从当前 legacy `.psy` 源生成测试夹具，v3 `.psy.rs` 另行编译。原生测试不能替代 v3 多用户链上验证；JavaScript E2E 的状态模型也不能证明节点共识安全。上述 staging 证据是普通流程和指定对抗场景的证据，不是全面审计结论。

## 未解决事项与验收标准

| 优先级 | 问题与当前影响 | 下一步 / 验收标准 |
| --- | --- | --- |
| **P0** | **PSY-20 不能单方即时撤销。** 合约 48 上 Bob 的花费证明锚定检查点 34798；Alice 的撤销于 34807 确认；Bob 在 34815 仍成功花费 40。合作关闭后退款在此场景正确，但 Bob 不关闭时未用额度会锁在托管槽中。不得把当前 API 宣传为 ERC-20 式 `approve` / `transferFrom`。 | 协议需要让授权相关的远程读取进入证明绑定的读集合，并按确定的交易顺序在确认时对照 Alice **当前**状态；读取已变化则拒绝并重新证明。验收：撤销先确认→旧证明和新花费均失败；花费先确认→Alice 只能退剩余额度；并发顺序确定且不能重复退款。协议实现由 `psy-node` 团队另行安排，本轮不改该仓库。 |
| **P1** | **PSY-20 私密转账已经在完整的 `.psy` 合约实现，但 staging v3 部署路径尚未包含它。** `token/src/main.psy` 的 `private_transfer` / `private_claim` 均可由 `psyup build` 编译，默认 `npm run build` 现同时生成并核对该产物；可部署的 `token/src/main.psy.rs` 仍缺这两个方法。两者 ABI 和存储布局不同。现有 0.1.1 v3 编译器对领取证明必需的 `get_session_proof_tree_root()` 直接报 `Unknown function`，也未提供对应的检查点用户树根接口。 | 完整 `.psy` 产物要接入兼容的部署工具链，或由 v3 工具链提供等价且受证明约束的根读取后移植；验收真实证明、重复领取拒绝、双用户 staging 兑换及余额守恒。现有部署需要单独迁移方案。不得仅加入会扣余额的私密存款入口。 |
| **P1** | **NFT 接收端活性与容量策略未定。** 接收者不认领、无法访问或持有槽已满时，发送者的队列位置可能长期占用；128 个持有槽、每方向 4 个未 ACK 转账以及 24 位 user ID 均为当前模板上限。 | 产品确定容量和无法认领时的处理规则；测试接收槽满、未注册/失联接收者和并发提交。任何取消或超时退款都要先证明历史读取下不会复制 NFT。需要更大容量时先设计新存储布局及迁移。 |
| **P1** | **PSY-721 没有 `approve` / operator 授权。** 目前仅持有者转出、接收者认领；市场代理挂单尚无合约接口。 | 如产品需要市场代操作，确定托管市场或链下意图与原子交换方案，单独设计授权和撤销安全模型。 |
| **P1** | **部署工具链未统一。** legacy `.psy` 与 staging v3 `.psy.rs` 路径不同；公开 0.1.1 CLI 曾因 `canonical layout verifier fingerprint mismatch` 无法部署 PSY-20，匹配节点源码构建的 CLI 可以部署。IDE 的 v3 部署流程未验证；直接 `psyup deploy` 绕过模板的 issuer 钱包预检。 | 固定与目标节点匹配的 CLI、编译 artifact 和部署协议；在干净环境验证 `deploy:checked`、IDE、钱包 user ID 校验和部署后 ABI。禁用或标明不受保护的直接部署路径。 |
| **P2** | **对抗测试及发行控制未闭环。** NFT 的历史证明和并发变体、节点对旧调用方叶子的拒绝、钱包/索引器读取四 Felt ID、CI 托管 runner、独立审计尚未完成。 | 运行可复现的链上对抗测试并保存交易与状态证明；对发布候选版本执行干净 CI、钱包/索引器集成、独立合约及协议审查、升级/迁移演练。 |

## 撤销问题的最短复现

当前合约 48 的两组 staging 对照测试：

1. Alice 撤销在检查点 **34749** 确认后，Bob **才生成**花费交易，合约报 `channel is not active`；最终 Bob 从该槽花费 0，Alice 取回全部 100。[拒绝记录](tests/live/evidence/2026-09-23/token-delegation-v3-active-guard-negative.json)
2. Bob 先在检查点 **34798** 生成花费证明；Alice 在 **34807** 撤销；Bob 之后提交同一证明，**34815** 仍确认花费 40。Bob 随后关闭，Alice 取回未用 60。[交易记录](tests/live/evidence/2026-09-23/token-delegation-v3-active-guard-historical-spend.json)

因此，`status == ACTIVE` 检查只挡住读到新状态的交易；把可用额度改成 0 或在 Alice 分区写 nullifier，同样不能让历史证明自动失效。当前节点的远程读取证明绑定的是交易锚定检查点，而非确认时的最新远程叶子。普通 `transfer/claim` 使用累计转出和接收方本地累计已领数，旧发送方读取在已测顺序中导致延后领取，没有产生重复领取；不能把这一结果外推为委托撤销安全。

## 推荐接手顺序

1. **先确定发布口径。** 将当前 PSY-20 委托标为实验性的“合作关闭托管通道”；主网标准授权和单方撤销保持阻断。16 个并行槽是已确定需求。
2. **协议团队设计当前远程读取校验。** 检查证明公开输入、跨 realm 聚合与确认顺序如何传递并校验读集合；先用上述旧证明用例作为失败测试，再验证并发与退款守恒。用户当前要求暂不修改 `psy-node`。
3. **合约团队在协议语义确定后重做撤销结算。** 明确 `request_revoke_delegation`、额度归零、已花费记账和未用额度退回的原子顺序；保留多对象并行授权需求。
4. **NFT 产品与集成团队明确容量、活性及市场授权需求。** 同时完成钱包、索引器、ABI 版本与迁移设计。
5. **最后做发布验证。** 统一工具链，运行完整链上对抗测试、干净 CI 与独立审计，再决定主网上线。

更完整的逐项发布门槛见 [PRODUCTION_READINESS.md](PRODUCTION_READINESS.md)。仓库中的 `tests/live/evidence/2026-09-23/` 只保存公开交易、检查点和状态证明；测试私钥及完整 witness 未提交。
