.DS_Store
*.log
node_modules/

# Contract build output
target/*
!target/*.json
!target/*.abi.json

# Configuration state markers (must never be committed or distributed with templates)
.issuer_configured
.psy-deploy-v3.json
